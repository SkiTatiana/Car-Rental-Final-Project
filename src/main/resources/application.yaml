spring:
  datasource:
    username: car_rental
    password: car_rental
    url: jdbc:mysql://localhost:3306/car_rental
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    
  sql: 
    init:
      mode: never
      